#Query qui incere dans location
INSERT INTO Permission;

SELECT P.idPermission AS idPermission
FROM Permission P
ORDER BY P.idPermission DESC
LIMIT 1;

INSERT INTO Location (idLocation, intitule)
VALUES (idPermission,nomLocation);
